import { Routes } from '@angular/router';
import { AuthSuccess } from '../components/component/auth-success/auth-success';

export const routes: Routes = [
    {
        path: 'auth-success',
        component: AuthSuccess
    }
];
